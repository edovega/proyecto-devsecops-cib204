# scripts/ — Utilidades del proyecto

> ⏳ Carpeta para scripts auxiliares del laboratorio.

## Scripts esperados (según la guía)

| Script | Función |
|---|---|
| `restaurar-archivos.sh` | Recrea los archivos ocultos (`.github/`, `.devcontainer/`) y hace push — se usa si el material se subió mal por el navegador |
| `iniciar-app.sh` | Entra a `app-movil/`, instala dependencias y arranca Expo web |

Estos scripts vienen en el material oficial (`proyecto-devsecops-cib204.zip`) y se integran aquí cuando esté disponible.