# Dependencias
node_modules/

# Nota didactica (rama inseguro): a proposito NO se ignoran .env ni *.pem,
# por eso quedan versionados y Gitleaks los detecta. En la rama solucion se
# agregan las lineas:
#   .env
#   *.pem
#   clave_privada*.pem
